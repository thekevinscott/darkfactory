[package]
name = "twilightfactory-cli"
version = "0.0.1"
edition = "2021"
license = "MIT"
authors = ["Kevin Scott <me@thekevinscott.com>"]
repository = "https://github.com/thekevinscott/twilightfactory"
description = "CLI for the twilight factory pattern: non-interactive agents converge specs into code without human review."

[lib]
name = "twilightfactory"
path = "src/lib.rs"

[[bin]]
name = "twilightfactory"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
